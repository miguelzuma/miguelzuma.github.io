source "https://rubygems.org"

# Matches the GitHub Pages build environment, so a local `bundle exec jekyll
# serve` reproduces what github.io publishes.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
end
